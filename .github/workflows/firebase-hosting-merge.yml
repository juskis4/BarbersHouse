# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
name: React CD for Prodution
on:
  push:
    branches: [ "master" ]
    paths:
      - 'frontend/**'
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    outputs:
      details_url: ${{ steps.deploy.outputs.details_url }}

    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BARBERSHOUSEPROJECT_419414 }}
          channelId: live
          projectId: barbershouseproject-419414
          entryPoint:  frontend

      - name: Set Preview URL as Secret
        run: |
          gh secret set FIREBASE_PREVIEW_URL --body "${{ steps.deploy.outputs.details_url }}" --repo "$GITHUB_REPOSITORY"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Output Preview URL   
        id: output_preview_url
        run: echo "🚀[Preview URL](${{ steps.deploy.outputs.details_url }})" >> $GITHUB_STEP_SUMMARY

      - name: Print Preview URL from Secret  
        run: |
          echo "Preview URL: ${{ secrets.FIREBASE_PREVIEW_URL }}"
